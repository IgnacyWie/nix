# Copy to .env on eta and fill secret values from Vaultwarden after the
# Keystone Service has been restored. Do not commit .env or real secrets.

LINKDING_HOST=bookmarks.mac.wie.dev
LINKDING_CSRF_TRUSTED_ORIGINS=https://bookmarks.mac.wie.dev
LINKDING_DISABLE_BACKGROUND_TASKS=False
LINKDING_SUPERUSER_NAME=ignacywie
LINKDING_SUPERUSER_PASSWORD=set-me
